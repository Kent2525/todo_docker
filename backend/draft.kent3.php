１月20日
■ EC CUBE
EC CUBEでJavascriptで顧客のクレジット情報などの情報をjsでサービス会社のサーバーに情報を投げて、専用トークンを取得する。
アクセスURLを変更に伴って、アクセスパスを変更する業務

■ OpenStreetMapで動いてしまう画像を静止画像にする。
OpenStreetMap + Leafletを使用しているがうまくいかない。

■ Toggl と backlogの設定
別メモ参照

1月21日
■ gitclone
backlogからgit cloneを行う。https通信で行う。
再pushの際にremote先変更後エラーが発生。ググって解決

■ OpenStreetMap + Leaflet
実際のローカル環境で実装。スタンダートな物は実装する事ができた。

■ EC CUBE
トークン先変更部分は見つける事はできたが、どういう仕組みで動いているのかわからない。
特にcontrollerの部分がどこでクラスが処理されているのかわからない。
